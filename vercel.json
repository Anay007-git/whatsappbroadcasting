{
  "buildCommand": "npm install && npm --workspace=packages/types run build && npm --workspace=packages/shared run build && npm --workspace=packages/validation run build && cd apps/web && npm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": null
}
